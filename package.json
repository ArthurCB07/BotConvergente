{
  "name": "convergencia-sinais",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm -w server run dev",
    "dev:web": "npm -w web run dev",
    "test": "npm -w server run test",
    "build": "npm -w web run build",
    "db:reset": "node -e \"const{rmSync}=require('node:fs');for(const f of ['server/data/app.db','server/data/app.db-wal','server/data/app.db-shm'])rmSync(f,{force:true});console.log('Banco apagado. O proximo start semeia o ambiente de demonstracao.')\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
